# WorkPortal 基本設計（権限・表示ルール）

このドキュメントは WorkPortal の**権限・表示の確定仕様（基本設計）**です。
2026-06-26 時点で実装・検証済みの内容を基準とします。

## 運用ルール（重要）
- 新しい改修命令が、この基本設計と**異なる／矛盾する**場合、実装前に必ず
  「**基本設計と異なる　よろしいですか？**」と確認すること。
- ユーザーが承認した場合のみ実装し、**この基本設計に追記・更新**する（末尾の「変更履歴」にも1行記録）。
- 基本設計に無い新規項目の追加は、確認のうえこのドキュメントへ追記する。

---

## 1. 認証・セッション
- 全ページ・全APIは**署名付きセッション必須**。Cookie `workportal_auth` は HS256 署名トークン（httpOnly）。
- 表示用に `workportal_user`（ユーザー名・非httpOnly）を別途保持。**認証には使わない（信頼しない）**。
- middleware が署名・有効期限を検証。未認証は **ページ＝SSOへリダイレクト／API＝401**。
- 検証済みユーザー名のみ信頼ヘッダ `x-wp-user` で下流へ。クライアント偽装ヘッダは除去。
- ログインは orgportal SSO 経由（既定 24時間保持。期限内はブラウザを閉じても自動ログイン）。
- ログアウトは `/api/auth/logout`（両Cookie破棄＋orgportalシングルログアウト）→ `/login`。

## 2. 役割の定義
- **取締役** = `isDirector` または `isRepresentative`（代表取締役）。承認・全閲覧権限の基準。
- **顧問** = `isAdvisor`。取締役会メンバーだが**承認権限なし**（権限上は社員相当）。
- **監査役** = `isAuditor`。**取締役会メンバー**。**閲覧は取締役と同等（全体可）**。ただし**承認・中止・削除は一切不可**（API・UI 両方でブロック）。**決定事項・実行タスクは取締役同様に作成・編集可**。**方針・プロジェクトは部長相当**（自部門のプロジェクト作成・編集可／方針作成不可／承認・削除不可）。orgportal 側 `Member.isAuditor`、admin 画面の「顧問」右隣に追加。
- **取締役会メンバー** = 取締役・代表取締役・顧問・監査役（`isBoardMember`）。
- **部長** = `position === 'manager'`。「担当部長」= 対象部門の部長（部門一致）。
- **課長** = `position === 'chief'`、**社員** = `position === 'staff'`。
- **管理者** = `role === 'admin'`（システム管理）。

## 3. 表示範囲（可視性）
- 既定ビュー：取締役以上＝**全体**／部長＝**自部門**／社員・課長＝**自分**。
- 決定事項・実行タスク・プロジェクト一覧：取締役＝全件／部長＝自部門に関連／社員＝自分が関与。
- **全社通達**（決定の `departmentId === 'all'`）＝**全員に表示**。
- **取締役会限定**（`boardOnly`）＝取締役会メンバー＋該当する担当部長のみ表示・作成。
- **方針ビジュアル**（/projects ビジュアルタブの方針ノード）＝**全員が全方針を閲覧可**。
  - ただし方針配下のプロジェクト・決定・タスクは従来どおり関与ベースで絞り込み。
  - **管理タブ**の方針一覧・権限は変更しない（従来スコープ）。
- 承認履歴（/history）：取締役会＝全件／部長＝自部門＋本人関与（**方針・PJは本人関与のみ**）／課長・社員＝本人関与のみ。
  - 「本人関与」= 決定:起案者/担当者/タスクwho/承認者、方針PJ:起案者/承認者のみ。

## 4. 承認マトリクス（方針・プロジェクト・決定事項）

**階層構造**：**方針 ＞ プロジェクト ＞ 決定事項 ＞ 実行タスク** の4階層。/projects のビジュアルタブでこの順にドリルダウン表示する（方針ごとのプロジェクト数→決定事項→タスクを展開）。方針タグ有り・プロジェクト未指定の決定は各方針配下に「（プロジェクト指定なし）」として表示。

**ビジュアルの受け皿ノード（実在レコードではない・表示専用）**：
- **「（方針未割当）」**＝方針(policy)が未設定のプロジェクトをまとめる受け皿。**実在の方針ではない**ため**管理タブの方針一覧には出ない**（正しい挙動）。配下のプロジェクト自体は管理タブの「プロジェクト」一覧に（方針空のまま）存在し、各プロジェクトの「方針」プルダウンで割り当て可能（担当部長＋取締役・割り当ては再承認）。
- **「（プロジェクト指定なし）」**＝方針には紐づくがプロジェクト未指定の決定事項をまとめる受け皿（各方針配下）。
- いずれも表示上のグループ見出しで、DBレコードではない。

**承認できる画面**：方針・プロジェクトの承認は **/projects の「管理」タブと「ビジュアル」タブの両方**から可能（権限者のみ承認ボタン表示。承認待ちは進捗 n/2・あと◯名を表示）。

**タグ選択UI（決定事項＋実行タスクのフォーム）**：方針・プロジェクトのタグ選択は **「プロジェクト」を上・「方針」を下** に表示。**プロジェクトを選ぶと紐づく方針が自動でチェック**され、**プロジェクトのチェックを外すと紐づく方針も外れる**（ただし同じ方針を持つ他の選択中プロジェクトが残っている場合は外さない）。各最大5。

**承認残数の判定（重要）**：承認・削除承認の「あと◯名」は**承認の“件数”ではなく“役職構成”で判定**する。
- 通常の決定事項＝**担当部長1名＋取締役1名（別々の人）**。例：取締役2名が承認しても担当部長が未承認なら成立せず「**あと1名（担当部長）**」。
- 全社通達・取締役会限定の決定／方針／プロジェクト＝**取締役2名（別々の人）**。
- 表示は不足している役職を明示（例「あと1名（担当部長）」）。進捗の「n/2」も件数でなく**充足数/2**で表す。
- 同ルールは**承認・削除承認の両方**、ダッシュボード「あなたの承認待ち」と各カードに適用（ヘルパー `approvalRemaining`）。

### 方針（Policy）
- 作成：**取締役のみ**
- 編集：**取締役のみ**（編集すると再承認）
- 承認：**取締役2名**（別々の人）
- 削除：削除申請→**取締役2名**承認で削除。直接削除API＝取締役のみ
- ビジュアルは全員閲覧可（§3）。

### プロジェクト（Project）
- 必須項目：**名称・説明・部門・担当者**（作成・編集とも空白不可）
- 作成：**担当部長（対象部門の部長）＋取締役**（部長は自部門のみ）
- 編集（名称・説明・部門・担当者）：**担当部長＋取締役**（編集すると再承認）
- 方針の付け替え／並び替え：**担当部長＋取締役**、かつ**再承認（取締役2名）**
- 承認：**取締役2名**（別々の人）。**部長は承認不可**
- 削除申請：**担当部長＋取締役**（実削除は取締役2名承認。担当部長の申請は人数に数えない）。直接削除API＝取締役のみ
- 画面：追加フォーム・編集・方針付け替え・削除申請は権限者のみ表示。一覧・カードに**部門・担当者を表示**。
- 担当者は**全メンバーから選択**（初期値＝入力者本人）。部門は取締役＝全部門選択可／部長＝自部門固定。
- **既存で部門・担当者が空のプロジェクト**は「未設定」と表示（一括バックフィルはしない）。編集時に必須入力を求める。

### 決定事項（Decision）
- 作成：ログイン者（担当部署で起案）。取締役会限定は取締役会メンバーのみ
- 編集：**入力者(起案者=createdBy)＋担当者(assigneeUsername)＋担当部長＋取締役**（編集すると再承認）
- 承認：**担当部長1名＋取締役1名**（別々の人）。**全社通達・取締役会限定は取締役2名**
- **全社通達(部門=all)の「担当部長」＝担当者(assignee)の部署の部長**とみなす（通常は決定の部門の部長）。
- **決定事項は「削除」ではなく「中止」**（2026-06-30 変更）。決定事項とその実行タスクは物理削除しない。
  - **中止**：申請できるのは**起案者・担当者・担当部長・取締役**。確定は**編集承認と同じ2名承認**（担当部長＋取締役1名／全社通達・取締役会限定は取締役2名）。承認が揃うと `archived=true`（中止確定）。
  - **中止解除（元に戻す）**：申請できるのは**担当者・担当部長・取締役**（起案者は不可）。確定は同じ2名承認。承認が揃うと `archived=false`＋ステータス再計算（タスク全完了→done／それ以外→approved）。
  - **データはフラグ操作のみ＝タスクをコピー・移動しない**ので、中止→復活で**完了タスクが2重化せず、紐づけも切れない**。
  - 中止中＝**アクティブ画面（決定一覧の未完了/すべて、実行タスクの稼働中、ダッシュボードの未完了・期限超過、方針ビジュアル、承認待ち）からは非表示**。
  - **完了済み実行タスクは完了のまま保持**し、ダッシュボードの完了集計・完了一覧(status=done/period)には引き続き含める。
  - 中止中の決定事項・実行タスクは **「中止一覧」(/cancelled・サイドメニュー)** に表示。表示範囲は承認履歴と同ルール（起案者・担当者・タスク担当・担当部署の部長・承認者など**自分に関係するものだけ／取締役以上は全件**）。同ページで中止解除を申請・承認できる。
  - **中止中は閲覧のみ・操作不可**：中止中の決定事項に紐づく実行タスクは、編集・ステータス変更・削除を**サーバー側でも403**で拒否。画面でもボタン非表示。
  - **紐づくリンクの誘導**：実行タスク等から決定事項リンクを開く際、中止中のものは **`/cancelled`（中止一覧）** へ誘導（アクティブは `/decisions?dec=`）。中止一覧ではタスクをクリックで5W1Hを**閲覧のみ**表示（編集不可）。
  - 実装：`Decision.archived`＝中止確定、`deleteRequested`＝申請中（archived の状態で中止/中止解除を区別）、Approval(action='delete') を双方向に流用。方針・プロジェクトは従来どおり「削除」（物理削除）。

#### 決定事項：表示・動作
- カードに **担当部門（「担当」）／担当者／起案者（「起案」）** を表示。承認待ちは **承認進捗 n/2・あと◯名**＋承認者名（取締役/担当部長）。自分が承認済みなら「✓承認済み」化（二重承認不可）。
- バッジ：全社通達＝**📢全員通達**、取締役会限定＝**🔒取締役会限定**、状態＝承認待ち/承認済み/完了。
- 編集すると承認リセット＝**再承認（pending）**。承認待ち中は **EditDiff** で変更内容（before→after・全項目）を表示。
- **編集取消**：承認待ちの間のみ・**編集した本人**のみ（編集前スナップショットに復元）。
- **承認取消**：自分の承認・承認待ち（2名未達）・**承認から30分以内**。
- **後追いタスク追加**：**部長以上（部長・取締役・管理者）**のみ。決定事項カードからの追加は**再承認**（追加タスクは承認まで非表示=pendingEdit）。実行タスクページからの追加は承認不要・即稼働。
- タスク0件かつ承認済みの決定は手動で**「完了／未完了」切替**（担当部長＋取締役・画面で制御）。タスクがある決定は**全タスク完了で自動「完了」**。

## 5. 実行タスク・個人タスクの編集/中止

### 用語
- **実行タスク** ＝ 決定事項に紐づくタスク（DecisionTask）。
- **個人タスク** ＝ 決定事項に紐づかないタスク（Todo）。

### 実行タスク（決定事項由来・全件共通）
- 編集・ステータス変更（未着手/進行中/完了）：**入力者(起案者=createdBy)＋担当者(who)＋担当部長＋取締役**
- **削除は無い。除去は「中止」**（物理削除しない）。中止＝決定事項の中止と同じルール：
  - 中止申請：**起案者・担当者・担当部長・取締役**／中止解除申請：**担当者・担当部長・取締役**（起案者は不可）。
  - 承認（中止・中止解除とも）：**担当部長＋取締役1名**（📢全社通達・🔒取締役会限定は**取締役2名**）。役職構成は決定事項と同一。承認の部門コンテキストは**タスク部門→無ければ決定部門**。
  - 中止確定＝`DecisionTask.archived`。中止中は**閲覧のみ**（編集・進捗変更・再承認をサーバーでも403）。**進捗・件数・自動完了の集計から除外**。
  - 承認データは `entityType='decisionTask'`・`entityId=taskId`・`action='delete'`（双方向。フラグは `deleteRequested`）。
  - 表示は**両方**：元の決定事項の中に「🚫中止中」（閲覧のみ＋中止解除）＋**中止一覧(/cancelled)** に「中止された実行タスク」セクション。ダッシュボードの承認待ち(MyWork)に中止/中止解除の承認待ちを表示。
- `createdBy`（入力者）は**作成時のみ設定。後から決定に紐づけても上書きしない／紐づけ者を作成者にしない**。
- **ステータス変更（進捗：未着手/進行中/完了）は再承認不要**。内容（5W1H・タグ）編集は**そのタスクのみ**親決定を再承認（pendingEdit）。編集取消は本人のみ（承認待ち中）。
- /todos の表示は**2段階展開**：①タスク→決定事項＋**決定事項作成者** ②「5W1H（実行タスク）」→ 5W1H＋**実行タスク作成者**＋編集（決定事項は残したまま）。方針/プロジェクトタグを付与・表示可。中止タスクは /todos の一覧から除外（中止一覧・決定事項カードで閲覧）。

### 個人タスク（Todo・共有含む）
- 編集・ステータス変更・削除：**担当者(所有者 userId)＋担当部長(所有者の部署の部長)＋取締役**
- 共有(isShared)タスクも同じ権限。編集で担当/部門を編集者にすり替えない（所有者のまま）。
- 項目：開始/完了予定/担当/部門＋**目的・手法詳細（旧 なぜ・どこで・どうやって を統合／§5C）**＋優先度＋公開設定(isShared)。工程(steps)はUI廃止。

### 共有タスク（/todos の「共有」タブ）
- 共有タブに出るもの＝**共有(isShared)の個人タスク**＋**全社通達の実行タスク（担当が自分でない＝自分タブに出ないもの）**。
- 全社通達の実行タスクは、who＝自分なら自分タブにも表示（重複集約のルール）。
- ダッシュボードの期限超過から来た時、自分タブに超過が無くても共有側に超過があれば
  「**共有タスクに（も）期限超過タスクがあります**」と案内バナーを表示。

## 5B. ダッシュボード（/dashboard）
ダッシュボードの各集計は、**ユーザーが /todos・/decisions で見られる範囲と一致**させる（`defaultView`：取締役＝全体／部長＝自部門／社員＝自分関与）。※過去に「ダッシュボードだけ全社集計」になっていた不具合を是正した重要ルール。期限超過の判定は §7（完了予定日 15:00 JST 超過）。

### 見た目（確定）
- 先頭の挨拶「おはようございます」は**表示しない**（日付のみ）。
- 「今日やること（リアルタイム）」の見出し・更新時刻表示は**出さない**（各枠は従来どおり30秒自動更新・no-store）。
- ダッシュボードの**強調色は藍(indigo)に統一**＝黄色(amber)のマーク（セクションの色ドット・件数バッジ・進行中の点・承認待ちバッジ等）は使わない。承認ボタンも藍。

### 上部「今日やること」（MyWork・30秒ごと自動更新／取得は no-store）
並び順（上から）：
1. **あなたの承認待ち**（全幅）：自分が承認すべき方針・プロジェクト・決定事項（**status=pending のみ**）。承認進捗・あと◯名（§4の役職構成判定）、その場で承認可。
2. **自分の未完了タスク ／ 自部門の未完了タスク**（2列）：
   - 自分＝自分の個人タスク（未完了）＋自分が担当(who)の決定タスク（未完了）。0件のとき 🏆「全タスク制覇！」。
   - 自部門＝自部門の決定タスク（未完了）。
3. **取締役会限定**（最下部・該当時のみ）：取締役会メンバー＝全件／担当部長＝本人 or 自部門のみ。
- 各リストは10件上限＋「すべて見る」。

### KPIカード
- **期限超過（タスク）**：個人タスク（自分/共有・未完了）＋表示範囲内の決定タスク（未完了）で 15:00 超過の件数。クリックで `/todos?overdue=1`。カードに「完了予定日の15:00を過ぎると超過になります」を表示。
- **期限超過（決定事項）**：表示範囲内の決定事項（未完了）で 15:00 超過の件数。クリックで `/decisions?overdue=1`。
- **完了実行タスク**：個人タスク＋決定タスクの `completedAt` を **本日／今週（月曜始まり）／今月／今年**で集計（JST境界）。
- **完了決定事項**：status=done の決定を `completedAt` で同期間集計。
- いずれも集計母数は上記スコープ（自分が見える範囲）。導入前に完了したもの（completedAt 無し）は集計外。
- **完了集計は中止済み（archived）決定の完了タスク／完了決定も含む**（完了実績を残す）。アクティブ系（未完了・期限超過）は中止済みを除外。
- **完了カードはクリック可能**：完了実行タスク→`/todos?status=done&period=<本日/今週/今月/今年>`、完了決定事項→`/decisions?status=done&period=…`。遷移先は **completedAt がその期間内の完了分**だけを表示（JST境界・§7同様）。
  - ※/todos は既定「自分」タブのため、共有(他人所有)の完了個人タスクは「共有」タブ側で確認する。

## 5C. 実行タスクの内容表示（共通）
決定事項(/decisions)・実行タスク(/todos)・中止一覧(/cancelled) のいずれでも、実行タスクは**クリックで全内容を閲覧**できる（**編集不可**）：内容・担当(誰が)・部門・開始日・完了予定日・**目的・手法詳細**・**方針・プロジェクト（格納タグ）**・作成者。**編集は「編集」ボタンから**のみ（編集すると再承認）。中止中の決定事項のタスクは閲覧のみ（操作不可）。

### 「どこで・なぜ・どうやって」→「目的・手法詳細」に統合（2026-06-30）
- 実行タスク（決定由来）・個人タスクとも、`どこで(whereLoc)・なぜ(why)・どうやって(how)` の3項目を**1つの自由記述テキストボックスに統合**。
- 入力欄＝**5行**のテキストエリア（プレースホルダ「目的・手法詳細など」・`resize-none`＋縦スクロール）。
- 保存は統合テキストを **`why`** に格納し、`whereLoc`/`how` はクリア（二重結合防止）。
- 既存（3項目に分かれて格納済み）データは、入力済みのものを**半角スペース3つ**で結合して表示・編集（空項目は除外）。共通ヘルパー `lib/taskDetail.ts` の `combineDetail`。
- 表示ラベルは「目的・手法詳細」。改行・空白は保持（whitespace-pre-wrap）。

## 5D. 全員通達一覧（/notices）
全社通達（決定の `departmentId === 'all'`）を一覧で恒久的に共有する閲覧ページ。サイドメニュー「全員通達一覧」（承認履歴の下・中止一覧の上）。
- 対象：**全社通達 かつ 中止(archived)でない かつ 決定済み（everApproved もしくは status=done）**。
- **期限切れ・完了になっても残す**（決めたことを全員で共有・確認し、なし崩し・忘却を防ぐ目的）。
- 全社通達は全員対象のため**全ログインユーザーが閲覧可**（役職制限なし）。
- 表示：状態バッジ（完了／期限超過／進行中=承認待ち）、起案者・担当者・完了予定日・完了日・タスク進捗。行クリックで紐づく実行タスク（内容・担当・完了予定・目的手法詳細・方針/PJ）を展開閲覧（編集不可）。
- 全員通達バッジは**赤系(rose)「📢 全員通達」**で、決定事項ページ(/decisions)のバッジも同色に統一。

## 6. 作成者・起案者の表示
- 決定事項：起案者 = `createdBy`（「起案」表示）。担当部署 = `departmentId`（「担当」）、担当者 = `assigneeUsername`（「担当者」）。
- 方針・プロジェクト：作成者は**「起案者」**と表記。
- 実行タスク：**実行タスク作成者（入力者）**を表示。決定由来タスクは**決定事項作成者＋実行タスク作成者の両方**を表示。
- 個人タスク：作成者 = 所有者。

## 7. 日付・期限（日本時間）
- 日付判定・日時表示はすべて **JST（Asia/Tokyo）**。
- **期限超過 = 完了予定日の 15:00（JST）を過ぎたら超過**（`isOverdueDue`）。
- 「今週締切」は超過分を除外。
- DB格納の入力日時（createdAt 等）の**値は変更しない**（表示のみJST）。

## 8. UI 表示ルール
- 権限の無いユーザーには、編集・削除・追加・承認の**ボタンを隠す/無効化**する（サーバーでも同権限で拒否＝二重防御）。
- カレンダーは**未実装**のためナビ無効（クリック不可・薄表示「準備中」）。メニュー順は最後＝**方針・プロジェクトの下**に配置。
- サイドメニューに **「中止一覧」(/cancelled)** を承認履歴の下に配置。
- **ダークモードは廃止**＝常にライト固定（`ThemeProvider` で `forcedTheme="light"`・システム連動無効）。サイドバーのテーマ切替ボタンは削除。
- ダッシュボード「あなたの承認待ち」は **status === 'pending'** のものだけ表示（承認済みは即時に消える）。
- ダッシュボード期限超過カードに「完了予定日の15:00を過ぎると超過になります」を表示。
- 「自分の未完了タスク」が0件のとき 🏆「全タスク制覇！」を表示。
- 失敗（保存・承認等）は**握りつぶさず画面にエラー表示**する。

## 9. データ保全・運用（補足）
- 本番とローカルは同一 Azure SQL を共有。スキーマ変更（db push）は本番即反映＝要承認。
- API取得は `cache: 'no-store'`（承認状態などを古く表示しない）。

## 10. 承認履歴（/history）の表示・記録
可視範囲は §3 を参照（取締役会＝全件／部長＝自部門＋本人関与・方針PJは本人関与のみ／課長・社員＝本人関与のみ）。本節は**表示内容と記録方式**を定める。

### 記録方式（AuditLog）
- 操作のたびに**追記専用の AuditLog** に記録：`approve`（承認）／`delete_approve`（削除承認）／`deleted`（削除実行）／`edit`（編集申請）。
- 記録時に**スナップショット**（対象名・申請者・申請日時・変更内容 before→after・関係者）を保存するため、**対象が削除された後も履歴は残る**。
  - 可視範囲スナップショット列：`deptIds`（部門）/`involved`（本人関与）/`assignee`（この内容の担当者）/`creator`（作成者）。
  - 既に削除済みのエンティティ由来の過去ログは関係者が埋まらない場合がある＝**今後分から正確**（既知の制約）。

### 行（一覧）に表示する項目
- **申請種別バッジ**（先頭）：`新規申請を承認` / `編集申請を承認` / `削除申請を承認` / `編集申請` / `削除を実行`。
- **作成: 〇〇**（`creatorNames`。方針・PJは「起案者」に相当）。
- **担当: 〇〇**（`assigneeNames` = この内容の担当者：決定の担当者＋実行タスクの who）。
- **申請: 氏名（部署）**（approve/delete_approve は申請者、edit は実行者本人）。
- **承認: 氏名（部署）**（approve / delete_approve のとき）。

### 詳細（行クリックで展開）に表示する項目
- 申請種別 / 対象 / **作成者** / **この内容の担当者** / 申請者 / 申請日時 / 承認者 / 承認日時 / **変更内容（before→after の全項目）**。
- 承認者欄には役職（取締役 / 担当部長）も併記。

### ラベル・用語
- 方針・プロジェクトの作成者は **「起案者」** と表記（決定事項は「担当」「起案」）。
- 実行タスク編集の変更内容は「実行タスク『名称』の〈項目〉」形式で、どのタスクの編集か分かるようにする。

### 更新
- **30秒ごと**の自動更新＋**ウィンドウfocus時**に再取得（承認直後の反映遅延を防ぐ）。
- 取得は `cache: 'no-store'`。

## 11. 集計分類（実行タスクのカテゴリ）
決定事項に紐づく実行タスク（`DecisionTask`）を集計目的で分類する仕組み。
- **データ**：`CategoryOption`（マスタ：`code`一意・`label`・`sortOrder`・`active`）。実行タスクに `DecisionTask.category`（= 選択した `code` を格納、未設定は null/空）。
- **管理ページ**：`/settings`（サイドメニュー「設定」）。**システム管理者（isAdminUser）のみ**表示・操作。
  - 追加（コード＋名称。コードは**半角英数・`-`・`_`のみ・一意**＝重複/誤作動防止、**作成後は変更不可**）。
  - 名称変更、**非表示／表示**（`active` 切替＝削除はせず履歴・既存値を保持）、**並び替え**（`sortOrder` を隣と交換）。
- **付与**：実行タスクの全フォーム（決定作成・決定内インライン編集/追加・/todos 編集）にプルダウン「集計分類（空白）＋active な分類」を表示。
- **表示マーク**：/decisions のタスク行・/todos のタスク行に **🏷 ＋名称**（violet系）バッジ。各タスクの詳細にも「集計分類」を表示。
- **絞り込み**：/todos に分類セレクト（「すべて／未設定／各分類」）。実行タスクのみ対象（個人タスクは対象外）。※並べ替え機能ではなく**絞り込みのみ**。
- 権限：GET は全ログインユーザー可。作成・変更（POST/PATCH）は**システム管理者のみ**（サーバーでも403）。

---

## 変更履歴（基本設計への追記ログ）
- 2026-06-26: 初版作成（このセッションまでの権限・表示ルールを集約）。
- 2026-06-26: §10「承認履歴（/history）の表示・記録」を追記（記録方式・行/詳細の表示項目・ラベル・自動更新）。
- 2026-06-26: §4 に方針・プロジェクトの詳細を追記（階層構造、ビジュアル＋管理の両タブで承認可、一覧に部門・担当者表示、担当者は全メンバー選択・部門は取締役全選択/部長自部門固定、既存空は「未設定」表示・バックフィルなし）。
- 2026-06-26: §4 決定事項に表示・動作（カード項目/バッジ、編集→再承認・EditDiff、編集取消=本人・承認取消=30分、後追いタスク追加=部長以上、手動完了=担当部長＋取締役、全タスク完了で自動完了）を追記。§5 に実行タスクの2段階展開・進捗は再承認不要・タスク単位再承認、個人タスクの項目構成、共有タスク（共有タブの内容・超過バナー）を追記。
- 2026-06-26: §5B「ダッシュボード」を追記（集計スコープ＝見える範囲と一致、MyWork 4枠、KPI：期限超過タスク/決定事項・完了実行タスク/完了決定事項の期間集計）。
- 2026-06-26: §4 にビジュアルの受け皿ノード（「方針未割当」「プロジェクト指定なし」は表示専用で管理タブには出ない）を追記。
- 2026-06-26: ダッシュボードの完了カードをクリック可能化（期間別の完了一覧へ遷移）。/todos・/decisions に `period` 絞り込み（completedAt が期間内）を追加。§5B に追記。
- 2026-06-26: ダッシュボードの見た目変更（挨拶「おはようございます」削除、「今日やること（リアルタイム）」見出し削除、黄色マークを全て藍に統一）。メニューのカレンダーを方針・プロジェクトの下へ移動。§5B・§8 に追記。
- 2026-06-30: 承認残数を「件数」から「役職構成」判定に修正（`approvalRemaining`）。不足役職を明示・進捗は充足数/2。§4 に追記。
- 2026-06-30: ダッシュボード上部の並び順を変更（①承認待ち=全幅 ②自分/自部門の未完了=2列 ③取締役会限定=最下部）。§5B に反映。
- 2026-06-30: 決定事項の削除で**完了実績を保持**＝完了を含む決定は物理削除せず `archived=true`（`Decision.archived` 列追加・db push 済）。アクティブ画面は非表示／完了集計・完了一覧には残す。§4・§5B に追記。
- 2026-06-30: 決定事項の「削除」を**「中止／中止解除」**に変更。中止＝2名承認(担当部長＋取締役・起案者/担当者/担当部長/取締役が申請可)→archived。中止解除＝2名承認(担当者/担当部長/取締役が申請可)→復活。フラグ操作のみでタスク2重化なし。完了タスクは完了維持・完了集計に残す。**「中止一覧」(/cancelled)ページ＋サイドメニュー**新設（承認履歴と同じ可視範囲）。§4・§5B・§8 に反映。E2E検証済（中止→archived/完了維持、復活→archived解除・tasks2件のまま・done1件維持・status再計算）。
- 2026-06-30: 中止中の決定事項・実行タスクは**閲覧のみ（サーバーでも編集/削除/ステータス変更を403）**、紐づくリンクは**中止一覧へ誘導**、中止一覧ではタスクをクリックで5W1H閲覧（編集不可）。§4 に追記。E2E検証済（中止中タスクPUT/DELETE→403）。
- 2026-06-30: **ダークモード廃止**（forcedTheme=light・切替ボタン削除）。§8 に追記。
- 2026-06-30: 実行タスクは**クリックで全内容を閲覧**（5W1H＋方針/プロジェクト＋作成者・編集不可、編集は編集ボタンから）に統一（/decisions はタスク行クリックで展開）。/cancelled の「決定事項を見る」リンク削除。§5C 新設。
- 2026-06-30: 「どこで・なぜ・どうやって」を**1つの自由記述「目的・手法詳細」に統合**（5行テキストエリア・既存3項目はスペース3つで結合・whyに格納）。実行タスク・個人タスク両方。§5・§5C に反映。
- 2026-06-30: タグ選択を**プロジェクト→方針の順**にし、**プロジェクト選択で紐づく方針を自動チェック／解除で自動オフ**（他の選択中PJが同方針を持つ場合は維持）。§4 に追記。
- 2026-06-30: **「全員通達一覧」(/notices) 新設**＋サイドメニュー追加。全社通達を期限切れ・完了後も恒久表示（全員閲覧可）。全員通達バッジを赤系(rose)に統一（/decisions も）。§5D 新設・§8 関連。
- 2026-06-30: **セキュリティ修正(#10 ログインCSRF)**：SSOログインに **state ノンス**を導入。middleware が `state` を発行し `wp_sso_state` Cookie(httpOnly/30分/単回)に保存＋`/authorize` へ渡す → orgportal が `state` をコールバックへ素通し → workportal コールバックが Cookie と照合し不一致なら `bad_state` で拒否。攻撃者が用意したトークンでの強制ログインを防止。E2E検証済（無Cookie/不一致→bad_state、一致+正規トークン→/dashboard、エラー経路はstate不要）。
- 2026-06-30: **セキュリティ修正(B 続き・身分一本化 8a)**：`isAdminUser` を**orgportal ディレクトリ基準に統一**（role==='admin' ＝ isSuperAdmin。ローカル `prisma.member` の影の台帳に依存しない・取得失敗時 fail-closed）。WorkPortal の `/api/members/[id]` PUT/DELETE を**410化**（メンバー管理は orgportal に一本化＝権限昇格・二重管理を解消）。E2E検証済（admin維持/非admin403/編集410）。
- 2026-06-30: **セキュリティ修正(B)**：⑤ToDo工程(steps)の追加/編集/削除に**親タスクの所有者認可**を追加（担当者・担当部長・取締役のみ。`[sid]` は `step.todoId===id` も検証＝IDOR封鎖）⑥カレンダーイベントAPIは**所有者モデルが無く未実装**のため**書込み(POST/PUT/DELETE)を403で封鎖**（読取GETは維持。実装時に `CalendarEvent.userId` で認可）⑦認証Cookie(`workportal_auth`/`workportal_user`/`workportal_admin`/`orgportal_session`)に**本番のみ `secure`** 付与⑧orgportal `/logout` の戻り先を**許可ホストのみ**に制限（オープンリダイレクト防止）⑨マジックリンクは**使用時に行削除＋期限切れ掃除**、`next` の `//`/`/\\` を弾く。E2E検証済。※メール存在の明示エラーは既存のUX方針どおり維持。
- 2026-06-30: **セキュリティ修正(A)**：①orgportal セッション鍵の既定値フォールバック撤廃＝fail-closed（`ORG_SESSION_SECRET` 必須）②orgportal セッショントークンに**有効期限(24h)を署名付与**（旧 `HMAC(username)` 無期限を廃止）③`/api/decisions/[id]/complete` に `canManageDecision` を追加（従来ログインのみ→入力者・担当者・担当部長・取締役のみ）④単独adminで2名承認を回避できた未使用ルート `/api/decisions/[id]/approve` を**削除**（承認は `/api/approvals` に一本化）。E2E検証済。
- 2026-06-30: **監査役（isAuditor）新設**（orgportal admin の「顧問」右隣・db push 済）。閲覧は取締役同等（全体）／**承認・中止・削除は一律不可**（/api/approvals・/api/deletions・物理DELETEルートで403、UI もボタン非表示）。決定事項・実行タスクは取締役同様に作成・編集可。方針・プロジェクトは**部長相当**（`canManageProject`/`canCreate('project')` で manager 扱い、自部門のみ／方針作成不可）。`isBoardMember`・`defaultView`・`ScopeControl`・audit 可視範囲・decisions/MyWork の isBoard に監査役を含め全件閲覧。§2 追記。E2E検証予定。
- 2026-06-30: **用語確定**：実行タスク＝決定事項に紐づくタスク／個人タスク＝紐づかないタスク。**実行タスクに「削除」は無い**（UIに削除ボタンが無く、孤立DELETE APIは無効だった）→ **「中止」に作り変え**。`DecisionTask.archived`/`deleteRequested` 追加（db push 済）。決定事項の中止と同じ2名承認（担当部長＋取締役／全社通達・取締役会限定は取締役2名）、双方向（中止/中止解除）、`entityType='decisionTask'` の Approval。中止中は閲覧のみ・集計から除外。表示は**決定事項の中（🚫中止中）＋中止一覧(/cancelled)の両方**、MyWork に承認待ち追加。/todos・dashboard・notices の集計から archived を除外。§5 改題・追記。E2E検証済（中止申請=部長→承認=取締役で archived、中止中PUT=403、中止解除=部長→取締役で復活）。
- 2026-06-30: 承認待ちの変更内容表示（`EditDiff`）を**実行タスク単位（taskId）でグループ化**＝複数タスクを編集したら**全タスク分の「どこを何に変えたか」を個別表示**（従来は editNote 上書きで最後の1件＋単一表示だった）。**集計分類の変更も差分に追加**（コード→名称、空は「（空白）」）。決定本体の編集でも承認待ち中の実行タスク差分（taskId付き）を保持して統合。E2E検証済（3タスク編集→3グループ＋各 DXpro→（空白）、同一タスク再編集→1グループに集約）。§4・§11 関連。
- 2026-06-30: **集計分類（実行タスクのカテゴリ）新設**。`CategoryOption` マスタ（code一意・active・sortOrder）＋ `DecisionTask.category`（db push・6件シード: key/cloud/dxpro/import/iso/board）。**/settings（システム管理者のみ）**で追加/名称変更/非表示/並び替え（コードは作成後不変・重複/誤作動防止）。全タスクフォームにプルダウン、/decisions・/todos に 🏷 バッジ表示、/todos に分類**絞り込み**（並べ替えではない）。§11 新設・§8 関連。API は GET=全員/POST・PATCH=管理者のみ（E2E検証済：GET200・dup409・badcode400・PATCH200・非管理者403）。
